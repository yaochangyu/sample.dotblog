# security-mechanisms-markdown-table 計畫

- [x] 步驟 1：將「請求通過，執行業務邏輯」段落下方的 HTML table 改寫成 Markdown table  
  原因：你明確要求該段落下方的 table 改用 Markdown 語法，需要只調整呈現格式，保留原本的欄位、資料內容與語意不變。

- [x] 步驟 2：更新 `tree.md` 並在完成後封存計畫檔  
  原因：這次會新增計畫檔，依專案規範需要同步維護專案結構文件，完成後移入 `.archive` 保留紀錄。
