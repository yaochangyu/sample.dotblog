# docs-sync 計畫

- [x] 步驟 1：修正執行環境設定中的埠號不一致  
  原因：`Program.cs` 與 `ValidateTokenAttribute.cs` 目前白名單包含 `7001`，但 `launchSettings.json` 的 HTTPS 埠是 `7026`，若不先統一實作，文件就無法正確對齊專案現況。

- [x] 步驟 2：同步更新 `README.md` 與 `spec.md`  
  原因：這兩份文件目前對預設網址、Token 預設值、速率限制、狀態碼與儲存方式的描述，和實作不一致，需要先修正為以程式碼為準。

- [x] 步驟 3：同步更新 `tree.md`  
  原因：`tree.md` 目前列出不存在的檔案，且測試案例數量與檔名描述也有落差，需要整理為符合實際目錄與現況。

- [x] 步驟 4：同步更新 `tests/security/security-test-plan.md`  
  原因：這份測試計畫目前列出不存在的腳本與錯誤的瀏覽器測試檔名，也有部分狀態碼描述與實作不一致，需要修正。

- [x] 步驟 5：同步更新 `blog.md` 的範例與敘述  
  原因：文章中的部分程式碼與目前專案實作不同，包含 CORS 白名單、TokenController 行為、驗證流程與測試數量，需一併對齊。

- [x] 步驟 6：更新 `tree.md`、封存計畫檔並確認結果  
  原因：完成後需要把這次計畫檔移入 `.archive`，並再次反映在 `tree.md`，讓文件狀態完整一致。
