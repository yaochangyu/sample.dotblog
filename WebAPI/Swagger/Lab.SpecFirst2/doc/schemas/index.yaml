parameters:
  petId:
    name: petId
    in: path
    required: true
    description: The id of the pet to retrieve
    schema:
      type: string

schemas:

requestBodies:
responses:
  okResponse:
    type: object
    properties:
      data:
        example: ok
        type: string
