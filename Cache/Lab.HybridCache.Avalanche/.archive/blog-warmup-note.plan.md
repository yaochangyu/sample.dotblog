# blog.md 預熱段落補充計畫

- [x] **步驟 1：補充預熱觸發時機**
  - 為何需要：把 `CacheWarmupService` 是在應用程式啟動時由 `IHostedService.StartAsync` 被呼叫的脈絡寫清楚，避免讀者誤以為它會在每次請求時執行。

- [x] **步驟 2：補充預熱策略**
  - 為何需要：把「預熱 hot keys，不是全量 key」的結論寫進文章，避免讀者以為所有重要 key 都要在啟動時全部預熱。

- [x] **步驟 3：同步目錄與封存**
  - 為何需要：文件修改完成後，要更新 `tree.md` 並封存計畫檔，讓專案文件狀態一致。
